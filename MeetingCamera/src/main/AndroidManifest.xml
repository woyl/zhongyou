<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zhongyou.meet.mobile.meetingcamera">

    <!-- 读写SD卡权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.zhongyou.meet.mobile.meetingcamera.activity.Camera1ByServiceActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <service android:name="com.zhongyou.meet.mobile.meetingcamera.service.Camera1Service" />
    </application>

</manifest>